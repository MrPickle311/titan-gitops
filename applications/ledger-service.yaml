apiVersion: apps/v1
kind: Deployment
metadata:
  name: ledger-service
  namespace: applications
  labels:
    app: ledger-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ledger-service
  template:
    metadata:
      labels:
        app: ledger-service
    spec:
      # WSPÓŁDZIELONY WOLUMEN W PAMIĘCI
      volumes:
        - name: pyroscope-agent
          emptyDir: {}
          
      # INIT CONTAINER: Pobiera agenta OTel i rozszerzenie Pyroscope
      initContainers:
        - name: fetch-otel-pyroscope
          image: busybox:latest
          command:
            - sh
            - -c
            - "wget -O /agent/opentelemetry-javaagent.jar https://github.com/open-telemetry/opentelemetry-java-instrumentation/releases/latest/download/opentelemetry-javaagent.jar && wget -O /agent/pyroscope-otel.jar https://github.com/grafana/otel-profiling-java/releases/latest/download/pyroscope-otel.jar"
          volumeMounts:
            - name: pyroscope-agent
              mountPath: /agent

      containers:
      - name: ledger-service
        image: registry.homelab/ledger-service:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8082
        
        # PODPIĘCIE AGENTA DO GŁÓWNEGO KONTENERA
        volumeMounts:
          - name: pyroscope-agent
            mountPath: /agent
            
        env:
        # MAGICZNA FLAGA JAVY: Automatycznie podpina agenta OpenTelemetry przy starcie
        - name: JAVA_TOOL_OPTIONS
          value: "-javaagent:/agent/opentelemetry-javaagent.jar"
          
        # ROZSZERZENIE PYROSCOPE DLA OPENTELEMETRY I KONFIGURACJA
        - name: OTEL_JAVAAGENT_EXTENSIONS
          value: "/agent/pyroscope-otel.jar"
        - name: OTEL_PYROSCOPE_START_PROFILING
          value: "true"
        - name: OTEL_SERVICE_NAME
          value: "ledger-service"
        - name: OTEL_EXPORTER_OTLP_ENDPOINT
          value: "http://tempo.monitoring.svc.cluster.local:4318"
          
        # KONFIGURACJA PROFILERA PYROSCOPE
        - name: PYROSCOPE_APPLICATION_NAME
          value: "ledger-service"
        - name: PYROSCOPE_SERVER_ADDRESS
          value: "http://pyroscope.monitoring.svc.cluster.local:4040"
        - name: PYROSCOPE_FORMAT
          value: "jfr"
        - name: PYROSCOPE_PROFILING_INTERVAL
          value: "10ms"
        - name: PYROSCOPE_PROFILER_EVENT
          value: "itimer"
        - name: PYROSCOPE_PROFILER_LOCK
          value: "10ms"
        - name: PYROSCOPE_PROFILER_ALLOC
          value: "512k"
        - name: POD_NAME
          valueFrom:
            fieldRef:
              fieldPath: metadata.name
        - name: PYROSCOPE_LABELS
          value: "pod=$(POD_NAME)"
          
        - name: SPRING_DATASOURCE_URL
          value: jdbc:postgresql://postgres-postgresql.databases.svc.cluster.local:5432/titan_db
        - name: SPRING_DATASOURCE_USERNAME
          value: postgres
        - name: SPRING_DATASOURCE_PASSWORD
          valueFrom:
            secretKeyRef:
              name: postgres-credentials
              key: postgres-password
        - name: KAFKA_BOOTSTRAP_SERVERS
          value: kafka.messaging.svc.cluster.local:9092
---
apiVersion: v1
kind: Service
metadata:
  name: ledger-service
  namespace: applications
  labels:
    app: ledger-service
spec:
  selector:
    app: ledger-service
  ports:
  - name: http
    port: 8082
    targetPort: 8082
---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: ledger-service-monitor
  namespace: applications
  labels:
    release: kube-prometheus-stack
spec:
  selector:
    matchLabels:
      app: ledger-service
  endpoints:
  - port: http
    path: /actuator/prometheus
    interval: 15s
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ledger-service-ingress
  namespace: applications
  annotations:
    kubernetes.io/ingress.class: nginx
    cert-manager.io/cluster-issuer: "homelab-ca-issuer"
spec:
  tls:
    - hosts:
        - ledger.homelab
      secretName: ledger-tls-secret
  rules:
  - host: ledger.homelab
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: ledger-service
            port:
              number: 8082
