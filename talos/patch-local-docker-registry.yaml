machine:
  registries:
    mirrors:
      registry.homelab:
        endpoints:
          - https://registry.homelab
    config:
      registry.homelab:
        tls:
          insecureSkipVerify: true # If your registry uses HTTPS with a self-signed certificate, you can also configure TLS settings under 

