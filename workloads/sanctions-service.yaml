apiVersion: apps/v1
kind: Deployment
metadata:
  name: sanctions-service
  namespace: applications
  labels:
    app: sanctions-service
spec:
  replicas: 1
  revisionHistoryLimit: 0
  selector:
    matchLabels:
      app: sanctions-service
  template:
    metadata:
      labels:
        app: sanctions-service
    spec:
      topologySpreadConstraints:
        - maxSkew: 1
          topologyKey: kubernetes.io/hostname
          whenUnsatisfiable: ScheduleAnyway
          labelSelector:
            matchLabels:
              app: sanctions-service
      volumes:
        - name: pyroscope-agent
          emptyDir: {}
      initContainers:
        - name: fetch-otel-pyroscope
          image: busybox:latest
          command:
            - sh
            - -c
            - "wget -O /agent/opentelemetry-javaagent.jar https://github.com/open-telemetry/opentelemetry-java-instrumentation/releases/latest/download/opentelemetry-javaagent.jar && wget -O /agent/pyroscope-otel.jar https://github.com/grafana/otel-profiling-java/releases/latest/download/pyroscope-otel.jar && wget -O /agent/pyroscope.jar https://github.com/grafana/pyroscope-java/releases/latest/download/pyroscope.jar"
          volumeMounts:
            - name: pyroscope-agent
              mountPath: /agent
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
            - matchExpressions:
              - key: node-role.kubernetes.io/control-plane
                operator: DoesNotExist
      containers:
      - name: sanctions-service
        image: registry.homelab/sanctions-service:latest
        imagePullPolicy: Always
        resources:
          requests:
            cpu: 100m
            memory: 256Mi
          limits:
            cpu: 500m
            memory: 512Mi
        ports:
        - containerPort: 9091
        volumeMounts:
          - name: pyroscope-agent
            mountPath: /agent
        env:
        - name: MANAGEMENT_ENDPOINTS_WEB_EXPOSURE_INCLUDE
          value: "*"
        - name: MANAGEMENT_ENDPOINT_ENV_SHOW_VALUES
          value: "ALWAYS"
        - name: JAVA_TOOL_OPTIONS
          value: -javaagent:/opt/byteman/lib/byteman.jar=listener:true,port:9288,address:0.0.0.0 -javaagent:/agent/pyroscope.jar -javaagent:/agent/opentelemetry-javaagent.jar
        - name: OTEL_JAVAAGENT_EXTENSIONS
          value: "/agent/pyroscope-otel.jar"
        - name: OTEL_PYROSCOPE_START_PROFILING
          value: "true"
        - name: OTEL_PYROSCOPE_ADD_PROFILE_URL
          value: "false"
        - name: OTEL_PYROSCOPE_ADD_PROFILE_BASELINE_URL
          value: "false"
        - name: OTEL_SERVICE_NAME
          value: "sanctions-service"
        - name: OTEL_EXPORTER_OTLP_ENDPOINT
          value: "http://tempo.monitoring.svc.cluster.local:4318"
        - name: OTEL_LOGS_EXPORTER
          value: "none"
        - name: OTEL_METRICS_EXPORTER
          value: "none"
        - name: LOGGING_PATTERN_LEVEL
          value: "trace_id=%X{trace_id} span_id=%X{span_id} %5p"
        - name: PYROSCOPE_APPLICATION_NAME
          value: "sanctions-service"
        - name: PYROSCOPE_SERVER_ADDRESS
          value: "http://pyroscope.monitoring.svc.cluster.local:4040"
        - name: PYROSCOPE_FORMAT
          value: "jfr"
        - name: PYROSCOPE_PROFILING_INTERVAL
          value: "1ms"
        - name: PYROSCOPE_PROFILER_EVENT
          value: "wall"
        - name: PYROSCOPE_PROFILER_LOCK
          value: "10ms"
        - name: PYROSCOPE_PROFILER_ALLOC
          value: "128k"
        - name: PYROSCOPE_LOG_LEVEL
          value: "info"
        - name: POD_NAME
          valueFrom:
            fieldRef:
              fieldPath: metadata.name
        - name: PYROSCOPE_LABELS
          value: "pod=$(POD_NAME),service_name=sanctions-service"
---
apiVersion: v1
kind: Service
metadata:
  name: sanctions-service
  namespace: applications
  labels:
    app: sanctions-service
spec:
  selector:
    app: sanctions-service
  ports:
  - name: grpc
    port: 9091
    targetPort: 9091
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: sanctions-service-ingress
  namespace: applications
  annotations:
    kubernetes.io/ingress.class: nginx
    cert-manager.io/cluster-issuer: "homelab-ca-issuer"
    nginx.ingress.kubernetes.io/backend-protocol: "GRPC"
spec:
  tls:
    - hosts:
        - sanctions.homelab
      secretName: sanctions-tls-secret
  rules:
  - host: sanctions.homelab
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: sanctions-service
            port:
              number: 9091

