apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: export-db-cluster
  namespace: databases
spec:
  instances: 2
  storage:
    size: 20Gi
    storageClass: "openebs-hostpath"
  bootstrap:
    initdb:
      database: payment_batch_db
      owner: app
