apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: limits-db-cluster
  namespace: databases
spec:
  instances: 2
  storage:
    size: 10Gi
    storageClass: "openebs-hostpath"
  bootstrap:
    initdb:
      database: limits_db
      owner: app
