apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: limits-db-cluster
  namespace: databases
spec:
  instances: 2
  certificates:
    serverCASecret: homelab-ca-bundle
    serverTLSSecret: limits-db-cluster-server-tls
  postgresql:
    pg_hba:
      - hostssl all all all scram-sha-256
      - host all all all reject
  monitoring:
    enablePodMonitor: true
  managed:
    services:
      additional:
        - selectorType: rw
          serviceTemplate:
            metadata:
              name: limits-db-cluster-rw-lb
            spec:
              type: LoadBalancer
              ports:
                - name: postgres
                  port: 5432
                  targetPort: 5432
  storage:
    size: 10Gi
    storageClass: "longhorn"
  bootstrap:
    initdb:
      database: limits_db
      owner: app
