apiVersion: postgresql.k8s.enterprisedb.io/v1
kind: Cluster
metadata:
  name: titan-db-cluster
  namespace: databases
spec:
  instances: 3
  storage:
    size: 50Gi
    storageClass: "openebs-hostpath"
  bootstrap:
    initdb:
      database: titan_db
      owner: app
